import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    chatPage: {
        width: "100%",
        overflow: "visible",
        marginTop: "15%",
        marginBottom: "5%"
    },
})

export default styles;