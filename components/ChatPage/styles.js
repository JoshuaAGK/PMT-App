import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    chatPage: {
        width: "100%",
        overflow: "visible",
        marginTop: "5%",
        marginBottom: "5%"
    },
})

export default styles;