import { StyleSheet, Dimensions } from 'react-native';

const styles = StyleSheet.create({
    mainPage: {
        width: "90%",
        overflow: "scroll",
        paddingTop: 80
    },
    bigText: {
        fontSize: 40,
        fontWeight: "600"
    }
})

export default styles;